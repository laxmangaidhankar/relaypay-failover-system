# relaypay-failover-system

Backend scaffold for the relaypay failover system.

Current structure includes:

- `src/` for app code
- `scripts/` for seed and load test entry points
- `tests/` for unit and integration coverage
- `.github/workflows/ci.yml` for CI wiring
